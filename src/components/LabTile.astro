<section class="ink-panel lab-tile">
  <p class="mono">FEATURED LAB</p>
  <h2>Command Palette Prototype</h2>
  <p>A keyboard-first way to move through the personal OS.</p>
  <a class="mono" href="/lab">OPEN LAB -></a>
</section>

<style>
  .lab-tile {
    padding: 16px;
    background:
      linear-gradient(135deg, color-mix(in oklch, var(--cyan) 14%, var(--paper)), var(--paper) 55%);
  }

  .lab-tile > p:first-child {
    color: var(--cyan);
    font-size: 12px;
    margin: 0 0 12px;
  }

  .lab-tile h2 {
    margin: 0;
    font-size: 1.4rem;
    line-height: 1.08;
  }

  .lab-tile > p:not(:first-child) {
    color: var(--muted);
    margin: 8px 0 0;
  }

  .lab-tile a {
    color: var(--blue);
    font-size: 12px;
    display: inline-block;
    margin-top: 12px;
    font-weight: 800;
  }
</style>
